import random 
import time
import os

lives = 3
score = 0
attempts = 3 

gps_code = str(random.radint(100,999)) #so only three digit numbers show up 

print("GPS code:", gps_code)
time.sleep(3)

if os.name == "nt":
